apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-app
    type: frontend
  namespace: qa
spec:
  selector:
    app: my-app
    type: frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  
  type: LoadBalancer